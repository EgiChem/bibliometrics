\documentclass{article}

\begin{document}

\title{Publication list}
\date{}
\maketitle

\section*{All publications}

\begin{enumerate}
    {% for pub in publications %}{% if pub.type == 'Article' %}
        \item {{ pub.author_names }}, {{ pub.title }}, \it{ {{pub.publicationName}} }, {{pub.volume}}, {% if pub.issueIdentifier %}{{pub.issueIdentifier}}, {% endif %}{% if pub.pageRange %}{{pub.pageRange}}, {% endif %}{{ pub.year }}. {% if include_doi %}DOI: {{pub.doi}}. {% endif %}{% if include_citations %}Citations: {{pub.citedby_count}}. {% endif %}
    {% endif %}{% endfor %}
\end{enumerate}


\end{document}
